bash tests/check.sh

1. Сумма параметров по таблице сходится с моделью
  [31m✗[0m сумма по таблице не сходится с sum(p.numel() for p in model.parameters())
      AssertionError: по таблице 162,826,560, а в модели 134,515,008 — похоже, tied embeddings посчитаны дважды (+28,311,552)

2. Расчётное число LoRA-параметров совпадает с peft
  [32m✓[0m r=8, q_proj+v_proj: 460,800; r=16, все линейные: 4,884,480;

3. Хуки снимаются: повторный прогон не копит их на модулях
  [31m✗[0m хуки не снимаются — handle.remove() отсутствует
      AssertionError: после первого прогона на модулях осталось хуков: 3

4. Три режима памяти дают разные числа (full FT > LoRA > инференс)
  [31m✗[0m режимы не различаются по памяти — меряется снимок в конце, а не пик
      inference=1031МБ full_ft=2647МБ lora=2647МБ
      AssertionError: full FT (2647 МБ) должен быть дороже LoRA (2647 МБ) — это не пик, а снимок

5. Проект запускается модулем: python -m src.inspect_model --probe inference
  [32m✓[0m код возврата 0, замер напечатал 1 строк(и)

6. В замере названа метрика памяти (и на ускорителе это не RSS)
  [32m✓[0m cpu: ru_maxrss, пик 1031 МБ

7. В docs/anatomy.md записаны условия замера
  [31m✗[0m в docs/anatomy.md нет условий замера или они не совпадают с params.yaml
      AssertionError: нет docs/anatomy.md — сначала make inspect

[31mПровалено проверок: 4[0m

make: *** [check] Error 1
